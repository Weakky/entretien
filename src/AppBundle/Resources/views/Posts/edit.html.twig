{% extends 'base.html.twig' %}

{% block body %}
    <h1>Post edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a  class="btn btn-default btn-sm" href="{{ path('post_index') }}">
                Back to the list
            </a>
        </li>
        <li>
            <a class="btn btn-danger btn-sm" href="/{{ path('post_delete', { 'id': post.id }) }}" type="submit">
                Delete
            </a>
        </li>
    </ul>
{% endblock %}
